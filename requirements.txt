# PatentHub v0 — Streamlit Cloud runtime (no sentence-transformers)
streamlit>=1.32,<2
lancedb>=0.6,<1
pyarrow>=18,<22
pydantic>=2,<3
httpx>=0.27,<1
fastembed>=0.3,<1
numpy>=1.26,<3
pandas>=2,<3

# Dev / CI
pytest>=8,<9
pytest-mock>=3,<4

# Offline ingest only: pip install -e ".[ingest]"